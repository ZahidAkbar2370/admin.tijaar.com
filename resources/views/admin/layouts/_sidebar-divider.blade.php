<div class="mt-4 mb-1 px-3 pt-3 border-t border-gray-100 first:border-t-0 first:mt-2 first:pt-0">
    @if (!empty($label))
        <p class="text-[11px] font-bold uppercase tracking-wider text-gray-400">{{ $label }}</p>
    @endif
</div>
